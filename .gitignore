.pio/
.pioenvs/
.piolibdeps/
.clang_complete
.gcc-flags.json
.vscode/
*.log
*.bin
*.elf
build/
c5_node/_build.bat
c5_node/_install.bat
c5_node/_monitor.ps1
c5_node/sdkconfig
c5_node/sdkconfig.old
c5_node/managed_components/
c5_node/dependencies.lock
meshtastic_research/

# Local reference copies (Marauder, Bruce, etc.) — not for redistribution
.refs/
_audit_raspyjack/

# Platform migration sidecar (workaround for local file reverts, regenerate from platformio.ini)
platformio.ini.migration

# pioarduino auto-generated manifest
src/idf_component.yml

# Staged release artifacts (binary + release-notes draft) — attach to GitHub release instead of committing
_release/

# EXCEPTION to the *.bin rule above: the web flasher under docs/ MUST
# track its bins or GitHub Pages has nothing to serve. Keep these
# specifically un-ignored.
!docs/flash/bin/**/*.bin
!docs/bin/**/*.bin

# Superpowers brainstorm session output (HTML mockups, server state) — local only
.superpowers/
